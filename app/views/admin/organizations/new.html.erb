<% content_for :title do %>
  <%=t(:text_new_organization)%>
<% end %>

<% content_for :body_class do %><%=t(:text_admin)%><% end %>

<% content_for :tabs do %>
  <%= render 'admin/tabs' %>
<% end %>

<div id="ltColumn">

<% form_for [:admin, @organization] do |f| -%>
  <%= show_flash_message %>
  <ul>
    <%= render 'form', :form => f %>
    <li>
      <button class="submit" id="user_submit" type="submit" value="Update" name="commit">
        <span><%=t(:text_submit)%></span>
      </button>
    </li>
  </ul>

<% end -%>
</div>

<%= render 'sidebar' %>
